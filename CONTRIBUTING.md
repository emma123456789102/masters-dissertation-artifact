# Contributing

-- Use branches for features and experiments: `feature/*`, `experiment/*`.
-- Run tests locally with `npm test` before opening PRs.
-- Keep large data out of the repository; use external storage and document how to fetch it.
